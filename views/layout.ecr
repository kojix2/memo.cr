<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Memo</title>
    <meta name="memo-token" content="<%= Memo.h(Memo::Security.token) %>">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, sans-serif; background: #f7f3d3; height: 100vh; overflow: hidden; }
        .app-container { display: flex; height: 100vh; }
        .sidebar { width: 300px; background: #f7f3d3; border-right: 1px solid #e6ddb3; display: flex; flex-direction: column; }
        .sidebar-header { padding: 20px; border-bottom: 1px solid #e6ddb3; background: #f5f1c9; }
        .sidebar-header h1 { font-size: 24px; color: #8b7355; margin-bottom: 15px; }
        .new-note-btn, .btn { padding: 10px 16px; background: #f0e68c; border: 1px solid #ddd467; border-radius: 6px; color: #8b7355; cursor: pointer; }
        .new-note-btn { width: 100%; font-size: 14px; }
        .new-note-btn:hover, .btn:hover { background: #ede071; }
        .notes-list { flex: 1; overflow-y: auto; padding: 10px 0; }
        .note-item { padding: 15px 20px; border-bottom: 1px solid #ede8c1; cursor: pointer; }
        .note-item:hover { background: #f5f1c9; }
        .note-item.active { background: #f0e68c; border-right: 3px solid #ddd467; }
        .note-title { font-size: 16px; font-weight: 500; color: #8b7355; margin-bottom: 5px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
        .note-preview { font-size: 13px; color: #a69780; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
        .note-date { font-size: 11px; color: #b8a888; margin-top: 5px; }
        .main-content { flex: 1; display: flex; flex-direction: column; background: #fefcf0; }
        .editor-header { padding: 20px; border-bottom: 1px solid #f0e68c; }
        .editor-title { width: 100%; border: none; background: transparent; font-size: 24px; font-weight: 600; color: #8b7355; outline: none; padding: 10px 0; }
        .editor-content { flex: 1; padding: 20px; }
        .editor-textarea { width: 100%; height: 100%; border: none; background: transparent; font-size: 16px; line-height: 1.6; color: #8b7355; outline: none; resize: none; font-family: inherit; }
        .editor-actions { padding: 15px 20px; border-top: 1px solid #f0e68c; display: flex; gap: 10px; }
        .btn-delete { background: #f4d03f; border-color: #f1c40f; }
        .btn-delete:hover { background: #f1c40f; }
        .empty-state { display: flex; align-items: center; justify-content: center; height: 100%; color: #c4b896; font-size: 18px; }
        .editor-title::placeholder, .editor-textarea::placeholder { color: #c4b896; }
        .sidebar-footer { padding: 15px 20px; border-top: 1px solid #ede8c1; }
        .settings-link { font-size: 12px; color: #c4b896; text-decoration: none; }
        .settings-link:hover { color: #8b7355; }
        @media (max-width: 768px) { .sidebar { width: 250px; } }
    </style>
</head>
<body>
    <div class="app-container">
        <%= content %>
    </div>
</body>
</html>
